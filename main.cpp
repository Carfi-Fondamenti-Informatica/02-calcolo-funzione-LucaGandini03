#include <iostream>
using namespace std;

int main() {
    float a = 0;
    float b = 0;
    float x = 0;
    float y = 0;
    float z = 0;
    cin >> a >> b >> x >> y;
    if (x < 0 < y) {
        z = (a*x*x)-(b*y);}
    else if (y <= 0 <= x) {
        z = (a*x*x)-(b*y);}
    else{
       z=(a*x)+(b*y*y);}
    cout<<z<<endl;
   return 0;
}
